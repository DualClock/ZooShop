<Window x:Class="ZooShop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ZooShop.ViewModels"
        xmlns:view="clr-namespace:ZooShop.Views"
        Title="{Binding CurrentTitle}" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3">
    <Window.Resources>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#D4D4D4"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,9"/>
            <Setter Property="Margin" Value="8,1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#14FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <view:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CatalogViewModel}">
            <view:CatalogView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SalesViewModel}">
            <view:SalesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:InventoryViewModel}">
            <view:InventoryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EmployeesViewModel}">
            <view:EmployeesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ClientsPetsViewModel}">
            <view:ClientsPetsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TasksScheduleViewModel}">
            <view:TasksScheduleView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ReportsViewModel}">
            <view:ReportsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SupportViewModel}">
            <view:SupportView/>
        </DataTemplate>

        <DataTemplate x:Key="NavItemTemplate">
            <Button Content="{Binding}"
                    Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}"
                    Style="{StaticResource NavButtonStyle}"/>
        </DataTemplate>
    </Window.Resources>
    <DockPanel>
        <!-- Боковая панель навигации -->
        <Border DockPanel.Dock="Left" Width="240" Background="#1E1E2D">
            <DockPanel Margin="0,24,0,0">
                <Border DockPanel.Dock="Top" Padding="20,0,20,24">
                    <StackPanel>
                        <TextBlock Text="ZooShop" FontSize="20" FontWeight="SemiBold"
                                   Foreground="White"/>
                        <TextBlock Text="Управление зоомагазином" FontSize="11"
                                   Foreground="#7A7A9E" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Информация о пользователе -->
                <Border DockPanel.Dock="Top" Padding="20,12" Margin="0,0,0,8"
                        Background="#2D2D44" CornerRadius="6" Width="200" HorizontalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="{Binding UserName}" FontSize="12" FontWeight="SemiBold"
                                   Foreground="White" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding UserRole}" FontSize="10"
                                   Foreground="#A0A0C0" Margin="0,2,0,8"/>
                        <Button Content="Выйти" FontSize="11" Padding="8,4"
                                Background="#E53E3E" Foreground="White" BorderThickness="0"
                                Cursor="Hand" Command="{Binding LogoutCommand}"/>
                    </StackPanel>
                </Border>

                <Border DockPanel.Dock="Top" Height="1" Background="#2D2D44" Margin="16,0,16,12"/>

                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8,0">
                    <ItemsControl ItemsSource="{Binding NavigationItems}"
                                  ItemTemplate="{StaticResource NavItemTemplate}"/>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- Основная область контента -->
        <ContentControl Content="{Binding CurrentView}"
                        Margin="24"/>
    </DockPanel>
</Window>
